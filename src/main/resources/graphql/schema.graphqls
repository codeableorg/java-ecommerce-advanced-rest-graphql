type Query {
  productById(id: ID!): Product
  productsByCategory(category: String!): [Product]
  products: [Product]
}

type Product {
  id: ID!
  name: String!
  description: String
  price: Float!
  category: String!
  available: Boolean!
  sales: Int!
}
